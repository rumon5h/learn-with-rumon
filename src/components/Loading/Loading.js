import React from 'react';

const Loading = () => {
    return (
        <div className="flex items-center justify-center mt-3">
        <h2>Loading...</h2>
      </div>
    );
};

export default Loading;